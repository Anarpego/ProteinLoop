# Plan: Live Agent Activity

1. Add an optional progress callback to the Sagents runtime and instrument real execution
   boundaries.
2. Add run-scoped LiveView activity state with stale-event protection.
3. Build a reusable live activity monitor for the immersive tank console and main mission surface.
4. Add responsive, reduced-motion-safe visual states for waiting, running, accepted, and failed
   work.
5. Update demo validation and documentation, then run full executable verification.

